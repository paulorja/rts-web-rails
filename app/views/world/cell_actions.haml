- terrain = Terrain.get_terrain(@cell.terrain_code)
- building = Building.get_building(@cell.building_code)

.content-title
  - if building.nil?
    = terrain[:name]
  - else
    = building[:name]
    = '('+@cell.user.login+')'

= 'x: '+@cell.x.to_s+' y: '+@cell.y.to_s
%br


- if building.nil?
  - if terrain[:code] == TERRAIN[:grass][:code]
    = link_to 'Construir Casa', '#'
- else

