#game-content-left

  #cell-layer-1.cell-layer
    - @cells.each do |cell|
      .sprite{class: Terrain.get_terrain(cell.terrain_code)[:css_class]}

  #cell-layer-2
    = #buildings

  #cell-layer-1.cell_layer
    - @cells.each do |cell|
      .link-sprite{obj_name: 'grama'}

#game-content-right
  %div Navegar Pelo Mapa
  %table
    %tr
      %td
      %td= link_to '/\\', world_zoom_c_path(@x, @y.to_i-1)
      %td
    %tr
      %td= link_to '<', world_zoom_c_path(@x.to_i-1, @y)
      %td
      %td= link_to '>', world_zoom_c_path(@x.to_i+1, @y)
    %tr
      %td
      %td= link_to 'V', world_zoom_c_path(@x, @y.to_i+1)
      %td
